{
  "name": "mread",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=10.9.0"
  },
  "scripts": {
    "api": "npm start --prefix ./api",
    "template": "npm start --prefix ./template",
    "manager:api": "npm start --prefix ./manager/api",
    "manager:front": "npm start --prefix ./manager/front",

    "build:template": "npm run build --prefix ./template"
  },
  "dependencies": {
    "api": "file:./api",
    "template": "file:./template",
    "manager": "file:./manager"
  }
}
